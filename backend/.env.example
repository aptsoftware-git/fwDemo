# LLM Configuration
LLM_MODE=local
# Options: local (Ollama) or claude (Anthropic)

# Local LLM (Ollama) Configuration
# OLLAMA_URL=http://192.168.19.21:11434/api/chat
# LLM_MODEL=qwen2.5-coder:32b
OLLAMA_URL=http://localhost:11434/api/chat
LLM_MODEL=qwen2.5:3b
LLM_TIMEOUT=60
LLM_MAX_WORKERS=4
LLM_MAX_ROWS=300

# Database Configuration
DATABASE_URL=postgresql://postgres:password@localhost:5432/frs_db
# Format: postgresql://username:password@host:port/database_name

# API Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
# Comma-separated list of allowed origins

# Data Directory (optional - defaults to relative path)
DATA_DIR=../data

# Demo Data Configuration
DEMO_BASE_PATH=C:\Anu\APT\apt\army\fortwilliam\code\fwDemo\data\FRS_cleaned
DEMO_FORMATION=Fmn D

# Logging
LOG_LEVEL=DEBUG
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
